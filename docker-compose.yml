version: '3'

services:
  app-part2:
    image: node:18
    working_dir: /app
    volumes:
      - .:/app
    command: sh -c "npm install && node app.js"
    ports:
      - "5002:5001"
    env_file:
      - .env
    container_name: todo-container-part2
